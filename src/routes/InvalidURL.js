import React from "react";

import styles from "./InvalidURL.module.css";

const InvalidURL = () => {
    return (
        <main className={styles['invalid-url']}>
            <p>There is nothing here!</p>
        </main>
    );
}

export default InvalidURL